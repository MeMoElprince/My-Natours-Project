extends base

mixin field(link, icon, name, active)
    li(class=`${active ? 'side-nav--active' : ''}`)
        a(href=`${link}`)
            svg
              use(xlink:href=`img/icons.svg#icon-${icon}`)
            | #{name}

block content 
    include _userAccountTemplate